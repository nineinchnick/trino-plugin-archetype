project:
  name: ${artifactId}
  description: This is a Trino connector
  license: Apache-2
  java:
    groupId: ${groupId}
    version: 17

files:
  artifacts:
    - path: "target/{{projectName}}-{{projectVersion}}.zip"

release:
  github:
    owner: ${githubOwner}
