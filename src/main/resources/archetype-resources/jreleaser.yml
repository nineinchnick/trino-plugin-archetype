project:
  name: ${artifactId}
  description: This is a Trino connector
  license: Apache-2
  java:
    groupId: ${groupId}
    version: 11

files:
  artifacts:
    - path: "target/{{projectName}}-{{projectVersion}}.zip"

release:
  github:
    owner: ${githubRepo.split("/").get(0)}
